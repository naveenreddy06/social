   lastuser = "<%= @chronicles.empty? ? "" : @chronicles.last.published_date %>";
   userId = "<%= @user.id %>";
   type = "<%= params[:type]  %>";
   page = "<%= @page %>";
   chronicleId = "<%= @chronicles.empty? ? "" : @chronicles.last.id %>";
  $("#chronicle_content").replaceWith("<div id='chronicle_content'><%= escape_javascript(render(params[:partial_name])) %></div>");
 setTimeout(function(){fetch_older_chronicles()},3000);
