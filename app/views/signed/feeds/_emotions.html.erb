<script>
function createFavorite(emotId) {
 $.ajax({
      url: "/signed/feeds/emotions_tag",
      data: {feed_id: emotionId, type_id: emotId, update_tag: {favorite: true} },
      dataType: 'script'
    });
}
</script>

<div id="myemotionsModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style = "width :320px">

  <div class="modal-header">
   <h6 id="myModalLabel">Emotions</h6>
  </div>
  <div class="modal-body">
   <div class="row-fluid">
     <% EmotionType.each do |et| %>
       <div class="span10">
         <%= et.name %><br><br>
         <% et.emotions.each do |emotion| %>
           <center><% img = image_tag("/img/Emotions/#{emotion.name.downcase}.png", :title => "#{emotion.name}") %>
    <a href="#" onClick="createFavorite($(this).attr('id'));" id=<%=emotion.id.to_s %>><%= img + " " + "#{emotion.name}" %></a>
    </center><br>
         <% end %>
      </div>
     <% end %>
   </div>
  </div>

</div>
